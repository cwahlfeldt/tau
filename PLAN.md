Role: You are an expert Systems Engineer specializing in Rust, Tauri v2, and asset-bundling pipelines.Task: Develop a standalone Rust CLI tool called wrap-it-up. The tool's primary purpose is to wrap a web application into a mobile or desktop app by pointing it at an index.html file.Core Logic: Dependency Discovery (The "Magic" Step): Parse: Scan the provided index.html for linked assets: , <link href>, <img>, <source>, and even url() references in <style> blocks. Resolve: Convert relative paths into absolute paths based on the index.html location. Collect: Copy the HTML and all discovered dependencies into a flat or mirrored structure within an ephemeral src-tauri context. Configuration Hierarchy: Priority 1: Command-line arguments. Priority 2: wrap-it-up.conf.json. Priority 3: Defaults (Debug build, Current Host OS). Zero-Signing Debug Workflow: Build in debug mode (--debug) by default to bypass Android/iOS signing requirements. Target the local host OS if no platforms are specified. Ephemeral Build Pipeline: Programmatically generate a minimal Tauri v2 project structure. Run the build using std::process::Command. Artifact Extraction: Move final binaries (.apk, .app, .exe) to a ./build directory and delete the temporary scaffolding.CLI Requirements: wrap-it-up <PATH_TO_INDEX_HTML>: The entry point for the process. --release: Triggers a signed release build (requires wrap-it-up.conf.json for keys). -p, --platform: Comma-separated list of targets.Technical Constraints: Performance: Use a fast HTML parser like lol-html or html5ever for dependency discovery. Safety: Handle missing assets gracefully (warn the user if a src file is 404). KISS: Keep the internal Tauri scaffold as vanilla as possible---no unnecessary frontend boilerplate.How the Asset Discovery Should Work (Internal Logic)To keep this "idiomatic and efficient," the binary should follow this internal flow:Phase LogicScrape Use a streaming HTML parser to find all src and href attributes.Filter Ignore external URLs (e.g., \[https://cdn.com/\](https://cdn.com/)...) and focus on local files.Map Create a FileMap of the source directory to the ephemeral frontend directory.Inject If an index.html uses absolute paths (like /js/app.js), the tool should rewrite them to relative paths in the temporary build folder to ensure Tauri can resolve them properly.Example wrap-it-up.conf.jsonIf the user wants more than the defaults:JSON{"name": "QuickApp","version": "1.0.0","identifier": "com.waffles.quickapp","include": \["assets/\*<em class="slate-italic">\*/\*</em>", "manifest.json"\],"build": {"output": "./release_binaries","platforms": \["android", "windows"\]}}Pro-Tip for the Build LogicSince you&#x27;re targeting mobile via Tauri v2, the binary should also check for the existence of the wasm32-wasi and the specific mobile targets (e.g., aarch64-linux-android) using rustup. If they are missing, it can automatically run rustup target add \[target\] before starting the build.</p></x-turndown>
